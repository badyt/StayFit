// configuration data
interface Config {
    SERVER_HOST: string;
    SERVER_PORT: number;
}

const config : Config = {
    SERVER_HOST: 'localhost',
    SERVER_PORT: 4000,
};

export default config;
